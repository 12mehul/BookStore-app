class Book {
  constructor(id, title, writer, coverImage, point) {
    this.id = id;
    this.title = title;
    this.writer = writer;
    this.coverImage = coverImage;
    this.point = point;
  }
}

module.exports = Book;
